<Window 
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Title="Generate 3D View References"
    Width="1000"
    Height="600"
    WindowStartupLocation="CenterScreen"
    ShowInTaskbar="False"
    ResizeMode="CanResize"
    Background="{DynamicResource WindowBackgroundBrush}">
    
    <!-- Styles will be loaded programmatically -->
    
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        
        <!-- Busy Indicator Overlay -->
        <Border x:Name="busyOverlay" Grid.RowSpan="4" Panel.ZIndex="10" Style="{DynamicResource BusyOverlayStyle}">
            <StackPanel Style="{DynamicResource BusyContentStyle}">
                <ProgressBar x:Name="busyProgressBar" Style="{DynamicResource BusyProgressBarStyle}"/>
                <TextBlock x:Name="busyTextBlock" Style="{DynamicResource BusyTextBlockStyle}" Text="Loading..."/>
            </StackPanel>
        </Border>
        
        <!-- Tool Information -->
        <StackPanel Grid.Row="0" Margin="0,0,0,10">
            <TextBlock Text="Generate 3D View References" Style="{DynamicResource HeaderTextBlockStyle}"/>
            <TextBlock Text="This tool places 3D View Reference families at the location and extent of selected views." 
                       Style="{DynamicResource BodyTextBlockStyle}" Margin="0,5,0,0"/>
        </StackPanel>
        
        <!-- View Categories -->
        <GroupBox Grid.Row="1" Header="View Categories" Style="{DynamicResource StandardGroupBoxStyle}">
            <WrapPanel x:Name="viewCategoriesPanel" Orientation="Horizontal" Margin="5">
                <!-- View category checkboxes will be added here programmatically -->
            </WrapPanel>
        </GroupBox>
        
        <!-- Views List -->
        <Grid Grid.Row="2">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            
            <!-- Search and filters -->
            <Grid Grid.Row="0" Margin="0,0,0,8">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <TextBox x:Name="searchTextBox" Grid.Column="0" Height="30"
                         Style="{DynamicResource PlaceholderTextBoxStyle}"
                         Tag="Search view name, category, scale, sheet..."
                         TextChanged="Filter_Changed"/>
                <ComboBox x:Name="sheetFilterComboBox" Grid.Column="1" Width="130" Height="30" Margin="8,0,0,0"
                          ToolTip="Filter on whether the view is placed on a sheet"
                          Style="{DynamicResource StandardComboBoxStyle}"
                          SelectionChanged="Filter_Changed"/>
                <ComboBox x:Name="referenceFilterComboBox" Grid.Column="2" Width="120" Height="30" Margin="8,0,0,0"
                          ToolTip="Filter on whether the view already has a 3D View Reference"
                          Style="{DynamicResource StandardComboBoxStyle}"
                          SelectionChanged="Filter_Changed"/>
                <ComboBox x:Name="sheetParameterComboBox" Grid.Column="3" Width="190" Height="30" Margin="8,0,0,0"
                          ToolTip="Show a parameter of the view's sheet as an extra column"
                          Style="{DynamicResource StandardComboBoxStyle}"
                          SelectionChanged="SheetParameter_Changed"/>
            </Grid>

            <!-- DataGrid Header -->
            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                
                <CheckBox x:Name="selectAllCheckbox" Grid.Column="0" Content="Select All" 
                          Checked="SelectAll_Checked" Unchecked="SelectAll_Unchecked"
                          Foreground="{DynamicResource TextBrush}" Style="{DynamicResource StandardCheckBoxStyle}"/>
                <CheckBox x:Name="showDepthCheckbox" Grid.Column="1" Grid.ColumnSpan="4" Content="Show view depth (box to far clip)"
                          HorizontalAlignment="Right"
                          ToolTip="Off: a thin plate on the cut plane. On: a box from the cut plane to the view's far clip or view depth."
                          Foreground="{DynamicResource TextBrush}" Style="{DynamicResource StandardCheckBoxStyle}"/>
            </Grid>
            
            <!-- DataGrid with Views -->
            <DataGrid x:Name="viewsDataGrid" Grid.Row="2" Style="{DynamicResource StandardDataGridStyle}"
                      SelectionMode="Extended" IsReadOnly="False"
                      PreviewMouseLeftButtonDown="ViewsDataGrid_PreviewMouseLeftButtonDown">
                <DataGrid.Columns>
                    <DataGridTemplateColumn Header="Select" Width="Auto">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <CheckBox IsChecked="{Binding IsSelected, UpdateSourceTrigger=PropertyChanged}" 
                                          HorizontalAlignment="Center"
                                          Foreground="{DynamicResource TextBrush}" Style="{DynamicResource StandardCheckBoxStyle}"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <DataGridTextColumn Header="View Name" Binding="{Binding ViewName}" Width="*" />
                    <DataGridTextColumn Header="View Category" Binding="{Binding ViewCategory}" Width="*" />
                    <DataGridTextColumn Header="View Scale" Binding="{Binding ViewScale}" Width="*" />
                    <DataGridTextColumn Header="Sheet Reference" Binding="{Binding SheetReference}" Width="*" />
                    <DataGridTextColumn x:Name="sheetParameterColumn" Header="Sheet Parameter"
                                        Binding="{Binding SheetParameterValue}" Width="*" Visibility="Collapsed" />
                    <DataGridTextColumn Header="Reference" Binding="{Binding ReferenceStatus}" Width="Auto" />
                </DataGrid.Columns>
            </DataGrid>
        </Grid>
        
        <!-- Action Buttons -->
        <TextBlock x:Name="countTextBlock" Grid.Row="3" HorizontalAlignment="Left" VerticalAlignment="Center"
                   Margin="0,10,0,0" Style="{DynamicResource BodyTextBlockStyle}"/>
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button x:Name="createButton" Content="Create / Update References" Click="CreateViewReferences_Click" Style="{DynamicResource StandardButtonStyle}"/>
            <Button x:Name="isolateButton" Content="Isolate references" Click="IsolateElements_Click" 
                    IsEnabled="False" Style="{DynamicResource StandardButtonStyle}"/>
            <Button x:Name="cancelButton" Content="Cancel" Click="Cancel_Click" Style="{DynamicResource SecondaryButtonStyle}"/>
        </StackPanel>
    </Grid>
</Window>