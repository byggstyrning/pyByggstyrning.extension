<Window xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Create Family Elevations" Width="920" Height="820" WindowStartupLocation="CenterScreen"
        Background="{DynamicResource WindowBackgroundBrush}">
    <!-- Styles will be loaded programmatically -->
    <Grid Margin="15">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,15">
            <TextBlock Text="Create Family Elevations" Style="{DynamicResource HeaderTextBlockStyle}" Margin="0,0,0,5"/>
            <TextBlock Text="Select categories and family instances. Section views use instance bounding box and facing orientation; views are placed on sheets in a grid layout."
                       Style="{DynamicResource BodyTextBlockStyle}" 
                       Foreground="{DynamicResource TextSecondaryBrush}" TextWrapping="Wrap"/>
        </StackPanel>

        <!-- Elevation Settings -->
        <GroupBox Grid.Row="1" Header="Elevation Settings" Margin="0,0,0,15">
            <Grid Margin="10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="20"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="20"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Row 1: Scale, Template, Type -->
                <StackPanel Grid.Row="0" Grid.Column="0" Margin="0,0,0,10">
                    <TextBlock Text="View Scale" Style="{DynamicResource LabelTextBlockStyle}" Margin="0,0,0,5"/>
                    <ComboBox x:Name="scaleComboBox" Style="{DynamicResource StandardComboBoxStyle}" Height="30"/>
                </StackPanel>

                <StackPanel Grid.Row="0" Grid.Column="2" Margin="0,0,0,10">
                    <TextBlock Text="View Template" Style="{DynamicResource LabelTextBlockStyle}" Margin="0,0,0,5"/>
                    <ComboBox x:Name="templateComboBox" Style="{DynamicResource StandardComboBoxStyle}" Height="30" MaxDropDownHeight="250"/>
                </StackPanel>

                <StackPanel Grid.Row="0" Grid.Column="4" Margin="0,0,0,10">
                    <TextBlock Text="Section Type" Style="{DynamicResource LabelTextBlockStyle}" Margin="0,0,0,5"/>
                    <ComboBox x:Name="elevationTypeComboBox" Style="{DynamicResource StandardComboBoxStyle}" Height="30">
                        <ComboBox.ItemTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding Name}" Foreground="{DynamicResource TextBrush}"/>
                            </DataTemplate>
                        </ComboBox.ItemTemplate>
                    </ComboBox>
                </StackPanel>

                <!-- Row 2: Name Prefix, Crop Offset -->
                <StackPanel Grid.Row="1" Grid.Column="0" Margin="0,0,0,0">
                    <TextBlock Text="Name Prefix" Style="{DynamicResource LabelTextBlockStyle}" Margin="0,0,0,5"/>
                    <TextBox x:Name="namePrefixTextBox" Style="{DynamicResource PlaceholderTextBoxStyle}" 
                             Tag="Enter view name prefix..." Height="30" Text="Elev - "/>
                </StackPanel>

                <StackPanel Grid.Row="1" Grid.Column="2" Margin="0,0,0,0">
                    <TextBlock Text="Crop Offset (mm)" Style="{DynamicResource LabelTextBlockStyle}" Margin="0,0,0,5"/>
                    <TextBox x:Name="cropOffsetTextBox" Style="{DynamicResource PlaceholderTextBoxStyle}" 
                             Tag="Padding around instance bbox..." Height="30" Text="300"/>
                </StackPanel>

                <StackPanel Grid.Row="1" Grid.Column="4" Margin="0,0,0,0" VerticalAlignment="Bottom">
                    <TextBlock Text="Crop uses axis-aligned bounding box of each instance." Style="{DynamicResource SecondaryTextBlockStyle}" TextWrapping="Wrap"/>
                </StackPanel>
            </Grid>
        </GroupBox>

        <!-- Sheet Placement Settings -->
        <GroupBox Grid.Row="2" Header="Sheet Placement" Margin="0,0,0,15">
            <Grid Margin="10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="20"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="20"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0" Grid.Column="0" Margin="0,0,0,10">
                    <TextBlock Text="Sheet Number Prefix" Style="{DynamicResource LabelTextBlockStyle}" Margin="0,0,0,5"/>
                    <TextBox x:Name="sheetPrefixTextBox" Style="{DynamicResource PlaceholderTextBoxStyle}" 
                             Tag="e.g. FE-" Height="30" Text="FE-"/>
                </StackPanel>

                <StackPanel Grid.Row="0" Grid.Column="2" Margin="0,0,0,10">
                    <TextBlock Text="Group By" Style="{DynamicResource LabelTextBlockStyle}" Margin="0,0,0,5"/>
                    <ComboBox x:Name="groupByComboBox" Style="{DynamicResource StandardComboBoxStyle}" Height="30"/>
                </StackPanel>

                <StackPanel Grid.Row="0" Grid.Column="4" Margin="0,0,0,10">
                    <TextBlock Text="Sort By" Style="{DynamicResource LabelTextBlockStyle}" Margin="0,0,0,5"/>
                    <ComboBox x:Name="sortByComboBox" Style="{DynamicResource StandardComboBoxStyle}" Height="30"/>
                </StackPanel>

                <TextBlock Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="5" 
                           Text="Grid layout: up to 20 viewports wide; gap rows between groups. Sheet created without title block."
                           Style="{DynamicResource SecondaryTextBlockStyle}" FontStyle="Italic"/>
            </Grid>
        </GroupBox>

        <!-- Category Selection -->
        <GroupBox Grid.Row="3" Header="Categories" Margin="0,0,0,10">
            <Grid Margin="10">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="120"/>
                </Grid.RowDefinitions>
                <TextBlock Grid.Row="0" Text="Include instances from these categories (scroll to see all):" Style="{DynamicResource LabelTextBlockStyle}" Margin="0,0,0,8"/>
                <Border Grid.Row="1" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="3">
                    <ListBox x:Name="categoryListBox" Background="{DynamicResource ControlBackgroundBrush}"
                             ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                             ScrollViewer.VerticalScrollBarVisibility="Auto">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <CheckBox Content="{Binding CategoryName}" 
                                          IsChecked="{Binding IsSelected, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                          Checked="categoryFilterCheckBox_Changed" Unchecked="categoryFilterCheckBox_Changed"
                                          Style="{DynamicResource StandardCheckBoxStyle}" Margin="2,2"/>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </Border>
            </Grid>
        </GroupBox>

        <!-- Instance Selection -->
        <GroupBox Grid.Row="4" Header="Instance Selection" Margin="0,0,0,10">
            <Grid Margin="10">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0" Margin="0,0,0,10">
                    <TextBlock Text="Search Instances" Style="{DynamicResource LabelTextBlockStyle}" Margin="0,0,0,5"/>
                    <TextBox x:Name="instanceSearchTextBox" Style="{DynamicResource PlaceholderTextBoxStyle}" 
                             Tag="Search by mark, category, family, type, level, comments..." Height="30"
                             TextChanged="instanceSearchTextBox_TextChanged"/>
                </StackPanel>

                <Grid Grid.Row="1" Margin="0,0,0,10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    
                    <CheckBox x:Name="selectAllCheckBox" Grid.Column="0" Content="Select All Filtered" IsChecked="True"
                              Checked="selectAllCheckBox_Checked" Unchecked="selectAllCheckBox_Unchecked"
                              Style="{DynamicResource StandardCheckBoxStyle}"/>
                    
                    <TextBlock x:Name="selectionCountText" Grid.Column="2" Text="0 instances selected"
                               Style="{DynamicResource SecondaryTextBlockStyle}" VerticalAlignment="Center"/>
                </Grid>

                <Border Grid.Row="2" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="3">
                    <DataGrid x:Name="instancesDataGrid"
                              SelectionMode="Extended"
                              CanUserAddRows="False"
                              CanUserDeleteRows="False"
                              CanUserSortColumns="True"
                              AutoGenerateColumns="False"
                              IsReadOnly="False"
                              GridLinesVisibility="None"
                              HeadersVisibility="Column"
                              BorderThickness="0"
                              Background="{DynamicResource ControlBackgroundBrush}"
                              RowBackground="{DynamicResource DataGridRowBackgroundBrush}"
                              HorizontalGridLinesBrush="{DynamicResource BorderBrush}"
                              VerticalGridLinesBrush="Transparent"
                              RowHeight="24"
                              FontSize="12"
                              SelectionUnit="FullRow"
                              CanUserResizeRows="False"
                              AlternatingRowBackground="{DynamicResource BackgroundLightBrush}"
                              VerticalScrollBarVisibility="Auto"
                              HorizontalScrollBarVisibility="Auto">
                        <DataGrid.Columns>
                            <DataGridTemplateColumn Header="Sel" Width="40">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <CheckBox IsChecked="{Binding IsSelected, UpdateSourceTrigger=PropertyChanged}"
                                                  HorizontalAlignment="Center"
                                                  Checked="instanceCheckBox_Changed" Unchecked="instanceCheckBox_Changed"
                                                  Style="{DynamicResource StandardCheckBoxStyle}"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                            <DataGridTextColumn Header="Category" Binding="{Binding CategoryName}" Width="120"
                                                SortMemberPath="CategoryName" CanUserSort="True"/>
                            <DataGridTextColumn Header="Mark" Binding="{Binding Mark}" Width="80"
                                                SortMemberPath="Mark" CanUserSort="True"/>
                            <DataGridTextColumn Header="Family" Binding="{Binding FamilyName}" Width="140"
                                                SortMemberPath="FamilyName" CanUserSort="True"/>
                            <DataGridTextColumn Header="Type" Binding="{Binding TypeName}" Width="120"
                                                SortMemberPath="TypeName" CanUserSort="True"/>
                            <DataGridTextColumn Header="Level" Binding="{Binding LevelName}" Width="100"
                                                SortMemberPath="LevelName" CanUserSort="True"/>
                            <DataGridTextColumn Header="Comments" Binding="{Binding Comments}" Width="*"
                                                SortMemberPath="Comments" CanUserSort="True"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </Border>
            </Grid>
        </GroupBox>

        <!-- Status and Buttons -->
        <Border Grid.Row="5" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="0,1,0,0" 
                Background="{DynamicResource BackgroundLightBrush}" Margin="-15,10,-15,-15" Padding="15,10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <TextBlock x:Name="statusText" Grid.Column="0" VerticalAlignment="Center"
                           Style="{DynamicResource StatusTextBlockStyle}"
                           Text="Ready to create family elevations"/>
                
                <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button x:Name="createButton" Content="Create Elevations" Width="140" Height="30" IsDefault="True" 
                            Style="{DynamicResource StandardButtonStyle}" Margin="0,0,10,0"
                            Click="createButton_Click"/>
                    <Button x:Name="cancelButton" Content="Cancel" Width="100" Height="30" IsCancel="True" 
                            Style="{DynamicResource SecondaryButtonStyle}"
                            Click="cancelButton_Click"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Window>
