<Window xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="3D Zone Configuration Editor" Width="1100" Height="900" WindowStartupLocation="CenterScreen">
    <!-- Styles will be loaded programmatically -->
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0">
            <TextBlock Text="3D Zone Configuration Editor" Style="{DynamicResource HeaderTextBlockStyle}"/>
            <TextBlock Text="Manage parameter mapping configurations for 3D Zones" Style="{DynamicResource BodyTextBlockStyle}" Margin="0,0,0,15"/>
        </StackPanel>

        <!-- Main Content with TabControl -->
        <TabControl Grid.Row="1" x:Name="tabControl">
            <!-- Configurations List Tab -->
            <TabItem Header="Configurations" x:Name="configsTab">
                <Grid Margin="10">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <ListView x:Name="configsListView" Grid.Row="0" Margin="0,0,0,10" SelectionMode="Single">
                        <ListView.View>
                            <GridView>
                                <GridViewColumn Header="Name" Width="200">
                                    <GridViewColumn.CellTemplate>
                                        <DataTemplate>
                                            <TextBlock Text="{Binding Name}" VerticalAlignment="Center" Margin="5,0"/>
                                        </DataTemplate>
                                    </GridViewColumn.CellTemplate>
                                </GridViewColumn>
                                <GridViewColumn Header="Source Categories" Width="200">
                                    <GridViewColumn.CellTemplate>
                                        <DataTemplate>
                                            <TextBlock Text="{Binding SourceCategoriesDisplay}" VerticalAlignment="Center" Margin="5,0"/>
                                        </DataTemplate>
                                    </GridViewColumn.CellTemplate>
                                </GridViewColumn>
                                <GridViewColumn Header="Mappings" Width="250">
                                    <GridViewColumn.CellTemplate>
                                        <DataTemplate>
                                            <TextBlock x:Name="mappingsTextBlock" TextWrapping="Wrap" VerticalAlignment="Center" Margin="5,0" Loaded="MappingsTextBlock_Loaded"/>
                                        </DataTemplate>
                                    </GridViewColumn.CellTemplate>
                                </GridViewColumn>
                                <GridViewColumn Header="Enabled" Width="60">
                                    <GridViewColumn.CellTemplate>
                                        <DataTemplate>
                                            <CheckBox x:Name="enabledCheckBox" IsChecked="{Binding Enabled, Mode=TwoWay}" 
                                                     HorizontalAlignment="Center" VerticalAlignment="Center"
                                                     Style="{DynamicResource ToggleSwitchStyle}"
                                                     Checked="EnabledCheckBox_Checked" Unchecked="EnabledCheckBox_Unchecked"/>
                                        </DataTemplate>
                                    </GridViewColumn.CellTemplate>
                                </GridViewColumn>
                                <GridViewColumn Header="On IFC Export" Width="100">
                                    <GridViewColumn.CellTemplate>
                                        <DataTemplate>
                                            <CheckBox x:Name="writeBeforeIfcCheckBox" IsChecked="{Binding WriteBeforeIfcExport, Mode=TwoWay}" 
                                                     HorizontalAlignment="Center" VerticalAlignment="Center"
                                                     Style="{DynamicResource ToggleSwitchStyle}"
                                                     Checked="WriteBeforeIfcCheckBox_Checked" Unchecked="WriteBeforeIfcCheckBox_Unchecked"/>
                                        </DataTemplate>
                                    </GridViewColumn.CellTemplate>
                                </GridViewColumn>
                                <GridViewColumn Header="Only Empty" Width="80">
                                    <GridViewColumn.CellTemplate>
                                        <DataTemplate>
                                            <CheckBox x:Name="onlyEmptyCheckBox" IsChecked="{Binding OnlyEmpty, Mode=TwoWay}" 
                                                     IsEnabled="{Binding WriteBeforeIfcExport}"
                                                     HorizontalAlignment="Center" VerticalAlignment="Center"
                                                     Style="{DynamicResource ToggleSwitchStyle}"
                                                     Checked="OnlyEmptyCheckBox_Checked" Unchecked="OnlyEmptyCheckBox_Unchecked"
                                                     Loaded="OnlyEmptyCheckBox_Loaded"/>
                                        </DataTemplate>
                                    </GridViewColumn.CellTemplate>
                                </GridViewColumn>
                            </GridView>
                        </ListView.View>
                    </ListView>

                    <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Left">
                        <Button x:Name="addButton" Content="Add New" Width="120" Height="30" Margin="0,0,10,0" Style="{DynamicResource SuccessButtonStyle}"/>
                        <Button x:Name="editButton" Content="Edit Selected" Width="120" Height="30" Margin="0,0,10,0" IsEnabled="False" Style="{DynamicResource StandardButtonStyle}"/>
                        <Button x:Name="deleteButton" Content="Delete Selected" Width="120" Height="30" Margin="0,0,10,0" IsEnabled="False" Style="{DynamicResource DangerButtonStyle}"/>
                        <Button x:Name="moveUpButton" Content="Move Up" Width="100" Height="30" Margin="0,0,10,0" IsEnabled="False" Style="{DynamicResource SecondaryButtonStyle}"/>
                        <Button x:Name="moveDownButton" Content="Move Down" Width="100" Height="30" IsEnabled="False" Style="{DynamicResource SecondaryButtonStyle}"/>
                    </StackPanel>
                </Grid>
            </TabItem>

            <!-- Add/Edit Configuration Tab -->
            <TabItem Header="Edit Configuration" x:Name="editConfigTab" IsEnabled="False">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <Grid Margin="10">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <!-- Row 0: Configuration Name -->
                        <TextBlock Text="Configuration Name:" Grid.Row="0" Grid.Column="0" Margin="0,10,10,5" VerticalAlignment="Center" Style="{DynamicResource LabelTextBlockStyle}"/>
                        <StackPanel Grid.Row="0" Grid.Column="1" Margin="0,10,0,5">
                            <TextBox x:Name="nameTextBox" Height="25" Style="{DynamicResource PlaceholderTextBoxStyle}" Tag="Enter configuration name..."/>
                            <TextBlock x:Name="nameErrorTextBlock" Text="Configuration name is required" 
                                      Foreground="Red" FontSize="11" Margin="0,2,0,0" Visibility="Collapsed"/>
                        </StackPanel>

                        <!-- Row 1: Categories Section - 2 Column Layout -->
                        <Grid Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2" Margin="0,15,0,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            
                            <TextBlock Text="Source Category:" Grid.Row="0" Grid.Column="0" Style="{DynamicResource SubheaderTextBlockStyle}" Margin="0,0,0,5"/>
                            <TextBlock Text="Target Filter Categories (optional):" Grid.Row="0" Grid.Column="1" Margin="5,0,0,5" Style="{DynamicResource SubheaderTextBlockStyle}"/>
                            
                            <Grid Grid.Row="1" Grid.Column="0" Margin="0,5,5,0">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>
                                <StackPanel Grid.Row="0" Orientation="Horizontal" VerticalAlignment="Center">
                                    <CheckBox x:Name="useLinkedDocumentCheckBox" Content="Use Linked Document" VerticalAlignment="Center" Style="{DynamicResource ToggleSwitchStyle}" Margin="0,0,10,0"/>
                                    <TextBlock Text="Linked Document:" VerticalAlignment="Center" Margin="0,0,5,0" Style="{DynamicResource SecondaryTextBlockStyle}"/>
                                    <ComboBox x:Name="linkedDocumentComboBox" Height="25" IsEnabled="False" Style="{DynamicResource StandardComboBoxStyle}" VerticalAlignment="Center"/>
                                </StackPanel>
                                <TextBlock x:Name="linkedDocumentErrorTextBlock" Grid.Row="1" Text="Linked document not found or not selected" 
                                          Foreground="Red" FontSize="11" Margin="0,2,0,0" Visibility="Collapsed"/>
                            </Grid>
                            
                            <StackPanel Grid.Row="2" Grid.Column="0" Margin="0,5,5,0">
                                <ListBox x:Name="sourceCategoriesListBox" Height="120" SelectionMode="Single" Style="{DynamicResource StandardListBoxStyle}" BorderThickness="1"/>
                                <TextBlock x:Name="sourceCategoryErrorTextBlock" Text="A source category must be selected" 
                                          Foreground="{DynamicResource ErrorBrush}" FontSize="11" Margin="0,2,0,0" Visibility="Collapsed"/>
                            </StackPanel>
                            <ListBox x:Name="targetFilterCategoriesListBox" Grid.Row="2" Grid.Column="1" Height="120" Margin="5,5,0,0" SelectionMode="Multiple" Style="{DynamicResource StandardListBoxStyle}">
                                <ListBox.ItemTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding}">
                                            <TextBlock.Style>
                                                <Style TargetType="TextBlock">
                                                    <Setter Property="FontWeight" Value="Normal"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding RelativeSource={RelativeSource AncestorType=ListBoxItem}, Path=IsSelected}" Value="True">
                                                            <Setter Property="FontWeight" Value="Bold"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </TextBlock.Style>
                                        </TextBlock>
                                    </DataTemplate>
                                </ListBox.ItemTemplate>
                            </ListBox>
                            
                            <StackPanel Grid.Row="3" Grid.Column="0" Margin="0,10,5,0">
                                <TextBlock Text="Sort Source Elements By (optional):" Margin="0,0,0,3" Style="{DynamicResource SecondaryTextBlockStyle}"/>
                                <ComboBox x:Name="sourceSortPropertyComboBox" Style="{DynamicResource StandardComboBoxStyle}"/>
                            </StackPanel>
                        </Grid>

                        <StackPanel Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="2" Margin="0,10,0,5">
                            <TextBlock Text="Parameter Mappings:" FontWeight="Bold" Margin="0,0,0,5"/>
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Left">
                                <Button x:Name="addParameterMappingButton" Content="Add" Width="80" Height="30" Margin="0,0,5,0" Style="{DynamicResource StandardButtonStyle}"/>
                                <Button x:Name="editParameterMappingButton" Content="Edit" Width="80" Height="30" Margin="0,0,5,0" IsEnabled="False" Style="{DynamicResource StandardButtonStyle}"/>
                                <Button x:Name="deleteParameterMappingButton" Content="Delete" Width="80" Height="30" IsEnabled="False" Style="{DynamicResource DangerButtonStyle}"/>
                            </StackPanel>
                        </StackPanel>
                        <StackPanel Grid.Row="3" Grid.Column="0" Grid.ColumnSpan="2" Margin="0,5,0,10">
                            <DataGrid x:Name="parameterMappingsDataGrid" Height="150"
                                      AutoGenerateColumns="False" CanUserAddRows="False" CanUserDeleteRows="False" IsReadOnly="True" SelectionMode="Single"
                                      Style="{DynamicResource StandardDataGridStyle}">
                                <DataGrid.Columns>
                                    <DataGridTextColumn Header="Source Parameter" 
                                                      Binding="{Binding SourceParameter}" 
                                                      Width="*"/>
                                    <DataGridTextColumn Header="Target Parameter" 
                                                      Binding="{Binding TargetParameter}" 
                                                      Width="*"/>
                                </DataGrid.Columns>
                            </DataGrid>
                            <TextBlock x:Name="mappingsErrorTextBlock" Text="At least one parameter mapping is required" 
                                      Foreground="Red" FontSize="11" Margin="0,2,0,0" Visibility="Collapsed"/>
                        </StackPanel>

                        <StackPanel Grid.Row="4" Grid.Column="0" Grid.ColumnSpan="2" Orientation="Horizontal" Margin="0,15,0,0">
                            <Button x:Name="backButton" Content="Back" Width="100" Height="30" Margin="0,0,10,0" Style="{DynamicResource SecondaryButtonStyle}"/>
                            <Button x:Name="saveButton" Content="Save Configuration" Width="150" Height="30" IsEnabled="False" Style="{DynamicResource SuccessButtonStyle}"/>
                        </StackPanel>
                    </Grid>
                </ScrollViewer>
            </TabItem>
        </TabControl>

        <!-- Status Bar -->
        <StatusBar Grid.Row="2">
            <StatusBarItem>
                <TextBlock x:Name="statusTextBlock" Text="Ready" />
            </StatusBarItem>
        </StatusBar>
    </Grid>
</Window>

