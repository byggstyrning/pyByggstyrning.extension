<Window xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Select Areas for 3D Zone Creation" Width="500" Height="600" WindowStartupLocation="CenterScreen"
        Background="{DynamicResource WindowBackgroundBrush}">
    <!-- Styles will be loaded programmatically -->
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Search Box -->
        <TextBox x:Name="searchTextBox" Grid.Row="0" Margin="0,0,0,10" 
                 Style="{DynamicResource PlaceholderTextBoxStyle}" 
                 Tag="Type to search areas..."
                 TextChanged="searchTextBox_TextChanged"/>

        <!-- List View -->
        <ListView x:Name="areasListView" Grid.Row="1" Margin="0,0,0,10" SelectionMode="Extended"
                  Background="{DynamicResource ControlBackgroundBrush}"
                  Foreground="{DynamicResource TextBrush}"
                  BorderBrush="{DynamicResource BorderBrush}"
                  BorderThickness="1">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding display_text}" VerticalAlignment="Center" Margin="5"
                              Foreground="{DynamicResource TextBrush}"/>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <!-- Buttons -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="createButton" Content="Create Zones" Width="120" IsDefault="True" 
                    Style="{DynamicResource StandardButtonStyle}"/>
            <Button x:Name="cancelButton" Content="Cancel" Width="100" IsCancel="True" 
                    Style="{DynamicResource SecondaryButtonStyle}"/>
        </StackPanel>
    </Grid>
</Window>

